<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Custom TextInputLayout style -->
    <style name="TextAppearance.App.TextInputLayout" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textColorHint">@color/colorHint</item>
        <item name="android:textSize">16sp</item>
    </style>


    <style name="CustomBottomNavigationTextAppearance" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textSize">12sp</item> <!-- Increase size for readability -->
        <item name="android:ellipsize">none</item> <!-- Ensure no truncation -->
        <item name="android:singleLine">false</item> <!-- Allows multi-line text -->
        <item name="android:maxLines">2</item> <!-- Limit to two lines -->
        <item name="android:gravity">center</item> <!-- Center the text within the space -->
        <item name="android:textColor">@color/colorAccent</item> <!-- Ensure text is visible -->
    </style>
</resources>